<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-sdk android:minSdkVersion="14" />

    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".leaveReviewDialog"></activity>
        <activity android:name=".BookTreatment" />
        <activity android:name=".businessPageByUser" />
        <activity android:name=".EditPrivateUserProfile" />
        <activity android:name=".ProfileScreenBusiness" />
        <activity android:name=".searchResults" />
        <activity android:name=".searchScreen" />
        <activity android:name=".resetPassword" />
        <activity android:name=".ProfileScreenPrivate" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".ReminderBroadcast"/>
    </application>

</manifest>